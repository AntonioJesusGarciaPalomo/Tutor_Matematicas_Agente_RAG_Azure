# Core Framework
gradio==4.44.0

# HTTP Client
requests==2.32.3
urllib3==2.2.2  # Dependencia de requests, versión específica por seguridad

# Utilidades
python-dotenv==1.0.1  # Para cargar variables de entorno

# Para mejor manejo de async (usado por Gradio internamente)
aiohttp==3.10.5
aiofiles==24.1.0

# Logging mejorado (opcional pero recomendado)
python-json-logger==2.0.7

# Para testing (opcional)
pytest==8.3.2
selenium==4.23.1  # Si quieres hacer tests del UI
webdriver-manager==4.0.2  # Gestión automática de drivers de Selenium