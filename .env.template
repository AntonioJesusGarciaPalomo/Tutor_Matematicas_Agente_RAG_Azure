# ================================================
# CONFIGURACIÓN - MATH TUTOR AI
# ================================================
# Instrucciones:
# 1. Copia este archivo a .env: cp .env.template .env
# 2. Completa los valores según tu configuración de Azure
# 3. Ejecuta: python setup-and-verify.py

# ================================================
# AZURE AI FOUNDRY
# ================================================
# Endpoint del proyecto AI
# Formato: https://PROJECT_NAME.services.ai.azure.com/api/projects/PROJECT_NAME
# O: https://HUB_NAME.LOCATION.api.azureml.ms/discovery/workspaces/PROJECT_NAME
PROJECT_ENDPOINT=

# Nombre del modelo desplegado
# Opciones: gpt-4o, gpt-4-turbo, gpt-35-turbo
MODEL_DEPLOYMENT_NAME=gpt-4o

# Nombre del agente (opcional, default: math-tutor-agent)
AGENT_NAME=math-tutor-agent

# ================================================
# AZURE STORAGE
# ================================================
# Nombre de la cuenta de almacenamiento (sin .blob.core.windows.net)
STORAGE_ACCOUNT_NAME=

# Nombre del contenedor para imágenes generadas
IMAGES_CONTAINER_NAME=images

# ================================================
# CONFIGURACIÓN DE DESARROLLO LOCAL
# ================================================
# Entorno de ejecución (local|azure|production)
ENVIRONMENT=local

# Modo debug (true|false) - Activa logs detallados
DEBUG=false

# URLs de servicios (para desarrollo local)
BACKEND_URI=http://localhost:8000
FRONTEND_URI=http://localhost:7860

# Puertos para desarrollo local
PORT=8000
FRONTEND_PORT=7860

# ================================================
# AZURE AUTHENTICATION (Opcional)
# ================================================
# Opción 1: Service Principal (más seguro para producción)
# Descomenta y completa si usas Service Principal
# AZURE_CLIENT_ID=
# AZURE_CLIENT_SECRET=
# AZURE_TENANT_ID=

# Opción 2: Azure CLI (desarrollo local)
# No requiere configuración, solo ejecutar: az login

# ================================================
# CONFIGURACIÓN AVANZADA (Opcional)
# ================================================
# Resource Group donde están los recursos
RESOURCE_GROUP=rg-aifoundry-tutor

# Región de Azure donde se despliegan los recursos
# Valores comunes: eastus, westeurope, swedencentral, northeurope
AZURE_LOCATION=swedencentral

# Azure Container Registry (para despliegue)
AZURE_CONTAINER_REGISTRY_ENDPOINT=

# Timeouts (en segundos)
STARTUP_TIMEOUT=30
CHAT_TIMEOUT=60
RETRY_ATTEMPTS=3

# CORS Origins permitidos (separados por comas)
ALLOWED_ORIGINS=http://localhost:7860,http://127.0.0.1:7860,*

# Logging
LOG_LEVEL=INFO
ENABLE_CORS=true

# Hot reload para desarrollo (true|false)
NO_RELOAD=false

# ================================================
# NOTAS IMPORTANTES
# ================================================
# - PROJECT_ENDPOINT y STORAGE_ACCOUNT_NAME son obligatorios
# - Para desarrollo local, asegúrate de ejecutar 'az login' primero
# - El MODEL_DEPLOYMENT_NAME debe coincidir con el modelo desplegado en AI Foundry
# - Si usas Docker, las URLs deben usar nombres de servicio en lugar de localhost
#   Ejemplo: BACKEND_URI=http://backend:8000